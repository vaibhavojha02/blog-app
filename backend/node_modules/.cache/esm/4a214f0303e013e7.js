let express,dotenv;_03e‍.w("express",[["default",["express"],function(v){express=v}]]);_03e‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);

dotenv.config();

const app = express();
app.use(express.json());

app.get('/' , (req,res) => {
  res.send('server is runnnning');
})
const PORT = process.env.PORT || 
app.listen(PORT,() => {
  console.log(`Your server is running on {PORT}`);
})